Task Scheduler with Priority and Dependencies

Features
- JSON task parsing
- Dependency graph
- Cycle detection
- Priority scheduling
- Worker thread pool

Run

g++ scheduler.cpp -std=c++17 -pthread -o scheduler
./scheduler